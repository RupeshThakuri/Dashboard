import ProfileCards from './profileCards';
import Layout from '../components/Layout';

const Profile = () => {
  return (
    <Layout>
        <ProfileCards/>
    </Layout>
  );
};

export default Profile;