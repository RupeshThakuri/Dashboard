export type ProjectDataType = {
    project_name : string,
    no_of_members: number,
    deadline: string,
    status: string,
    updates: string,
    requirements: string,
    future_plans: string,
    date: string,
}